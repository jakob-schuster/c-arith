# c-arith
toy arithmetic language, implemented in c because i've only written language implementations in rust and i just needed a little c detox

make and run the code (though the exercise was more to write and read the code):
```
gcc intexp.c; ./a.out
```